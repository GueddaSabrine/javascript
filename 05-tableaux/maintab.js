//Les tableaux

//Declaration
let stagiaire = [`Roxana`, `Talia`, `Sabrine`, `Bakary`, `Souly`, `Aoukas`, `Toulepi`, `Badji`];
let numero = Array(item 1, 35, 84, -98);
let tabMixe = new Array(item true, 68, `chat`, null, [45, `Joachim`])

//Taille du tableau
let taille = stagiaires.length;
console.log(`tailleStagiaire = ${taille}`);
console.log(`tailleNumero = ${numero.length}`);
console.log(`tailleTabMixed = ${TabMixed.length}`);

//Acces aux elements du tableau
console.log(stagiaires[0]);
console.log(stagiaires[2]);

//Modifier les elements du tableau
stagiaire[8] = `Tayachi`
console.log(stagiaires[8]);

stagiaire[8] = `Guedda`
console.log(stagiaires[2]);

//Methodes de tableau

let numeros = [1, 2, 3, 4];
console.log(numeros);

//ajout en fin de tableau
numeros.push(-5);
console.log(numeros);

//supprime le dernier element du tableau
numeros.pop()
console.log(numeros);

//Ajout en debut de tableau
numeros.unshift(items -5);
console.log(numeros);

//supprime le premier element du tableau
numeros.shift();
console.log(numeros);